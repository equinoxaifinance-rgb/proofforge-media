{
  "name": "proofforge-cloudflare-container",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir dist && node --test test/*.test.mjs",
    "deploy": "wrangler deploy --strict"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
